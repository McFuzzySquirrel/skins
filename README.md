# skins
Fun collection of skins for various UI things.

- `vs-code/`: VS Code theme based on the hooked-on-hooks dashboard and mcfuzzysquirrel.github.io blog palette.
- `copilot-cli/`: terminal color presets to match the same Warm Hooks palette for Copilot CLI usage.
